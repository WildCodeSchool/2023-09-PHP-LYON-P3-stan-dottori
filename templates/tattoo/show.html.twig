{% extends 'base.html.twig' %}

{% block title %}Tattoo
{% endblock %}

{% block body %}
	{% include 'home/_navbar.html.twig' %}
	<div class="card-show">
		<a href="{{ path('app_tattoo_show', {'id': tattoo.id}) }}"><img src="{{ vich_uploader_asset(tattoo) }}" class="card-img-top" alt="{{ tattoo.id }}"></a>
		<div class="card-body">
			<h5 class="card-title">Tattoo name</h5>
			<p class="card-text">Description</p>
		</div>
                <div class="col">
						<p class="card-text">
							<h6>Realised date:
								{{ tattoo.madeOn ? tattoo.madeOn|date('d-m-Y') : '' }}</h6>
							<h6>Price:
								{{ tattoo.price }}€</h6>
						</p>
	</div>
	</div>
    <br>
	<button type="button" class="btn btn-outline-secondary"><a href="{{ path('app_tattoo_index') }}">back</a></button>
	{% if is_granted('ROLE_ADMIN') %}
		<button type="button" class="btn btn-outline-warning"><a href="{{ path('app_tattoo_edit', {'id': tattoo.id}) }}">edit</a></button>
		{{ include('tattoo/_delete_form.html.twig') }}
	{% endif %}
{% endblock %}
